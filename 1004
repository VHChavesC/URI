#include <iostream>
#include <stdio.h>
#include <math.h>

using namespace std;
int A, B, PROD;

int main() {

scanf("%i\n", &A);
scanf("%i\n", &B);

PROD = (A*B);

printf("PROD = %i\n", PROD);

    return 0;
}
