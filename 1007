#include <iostream>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>

using namespace std;

int A, B, C, D, DIF;

int main() {
    
 scanf("%i", &A);
 scanf("%i", &B);
 scanf("%i", &C);
 scanf("%i", &D);
 
 DIF = ((A*B)-(C*D));
 
 printf("DIFERENCA = %i\n", DIF);
    return 0;
}
